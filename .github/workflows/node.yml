name: Send OTP & Post Form

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *" # Every 15 minutes

jobs:
  otp_job:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout Repo
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: 📥 Install Dependencies
        run: npm install

      - name: 🚀 Run OTP Sender
        run: node index.js
